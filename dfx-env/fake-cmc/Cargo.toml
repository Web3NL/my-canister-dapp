package = { name = "fake-cmc-workspace-shim", version = "0.0.0", publish = false }

# This file intentionally minimal; the real crate is under fake-cmc/.
# Workspace is defined at dfx-env/Cargo.toml.
