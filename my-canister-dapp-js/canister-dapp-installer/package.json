{
  "name": "canister-dapp-installer",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "format": "prettier --check src test",
    "format:fix": "prettier --write src test",
    "lint": "eslint src test",
    "lint:fix": "eslint src test --fix",
    "test": "vitest --run",
    "typecheck": "tsc --noEmit"
  },
  "type": "module"
}
